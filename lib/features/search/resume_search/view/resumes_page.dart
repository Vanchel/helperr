import 'package:flutter/material.dart';

class ResumesPage extends StatelessWidget {
  const ResumesPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Тут пока ничего нет, но это страница для резюме'),
      ),
    );
  }
}
